version: '3.3'
services:
  sslchecker:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      HOOK_URL: "https://hooks.slack.com/services/XXXXXXXXXXXXXXXXXXXXXXXXXX"
      SSL_CHECK_HOST_LIST: "fivexl.io"
      CERTIFICATE_REMAINING_DAYS: "7"